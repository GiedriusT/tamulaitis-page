import { ProjectMedia } from '../../types';
import thumbUrl from './social-sequencer.jpg';
import videoUrl from './social-sequencer.mp4';
import articleUrl from './README.md';

const projectMedia: ProjectMedia = {
  thumbUrl,
  videoUrl,
  articleUrl,
};

export default projectMedia;
